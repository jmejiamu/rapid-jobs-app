export interface Rooms {
  jobId: string;
  otherUserId: string;
  lastMessage: string;
  timestamp: string;
  lastMessageSenderId: string;
  jobTitle: string;
}
export interface RoomDetails {
  jobId: string;
  otherUserId: string;
}
